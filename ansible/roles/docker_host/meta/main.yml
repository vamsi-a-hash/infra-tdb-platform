---
galaxy_info:
  role_name: docker_host
  author: TalkingDB
  description: Install Docker Engine and Compose plugin, configure the daemon, create talkingdb-net, and authenticate to the container registry.
  license: AGPL-3.0
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions:
        - jammy
        - noble
dependencies: []
