---
# Where the rendered env_file lands; deploy_services mounts it via env_file.
secrets_env_file: "{{ deploy_base_dir }}/config/.env"

# Pin the Infisical CLI version in production (empty = latest available).
secrets_infisical_version: ""
