---
galaxy_info:
  role_name: secrets
  author: TalkingDB
  description: Install the Infisical CLI and render runtime secrets to an env_file via machine-identity export.
  license: AGPL-3.0
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions:
        - jammy
        - noble
dependencies: []
